hash {
  key: value
}


CarNumber:LapNumber {
    StintNumber : Int
    TyreAge : Int
    TyreType : Str
    LapTime : Str
    Sector[1/2/3]Time : Str
    PitIn : Bool + Timestamp
    PitOut : Bool + Timestamp
    Sector[1/2]SpeedTrap : Float
    FinishLineSpeedTrap : Float
    BackStraightSpeedTrap : Float
}

CarNumber {
    CurrentLap : Int
    NumberOfPitstops : Int
}

Session {
    CurrentLap : Int
    TotalLaps : Int
    StartTime : Timestamp
    EndTime : Timestamp
}

# lap attribute is kept to the last since it is optional. Practice & Quali sessions do not have laps associated with RCMs
RaceControlMessages {
    MessageNum : Str (Other,Utc,Message,Lap)
    MessageNum : Str (Drs,Utc,Status,Lap)
    MessageNum : Str (SafetyCar,Utc,Mode,Status,Message,Lap)
    MessageNum : Str (CarEvent,Utc,RacingNumber,Message,Lap)
    MessageNum : Str (Flag,Utc,FlagType,Scope,Message,Lap)
}
